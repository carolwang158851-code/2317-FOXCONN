# P1008 Formula Registry v1.2

Version 1.2 is an additive governance-only revision in the canonical
`contracts/p1008_formula_registry/` lineage. The hash-governed v1.0 and v1.1
snapshots remain immutable.

This revision records the completed independent `MIDR.UD` research review and
the Owner decision to keep current production unchanged:

- design intent is `NOT_PROVEN`;
- a defect is `NOT_PROVEN`;
- the current formula is not asserted to be optimal or semantically proven;
- formula change is not justified by the available evidence;
- current UD values remain `NORMAL=1.00`, `CAUTION=0.90`, and
  `SYSTEMIC=0.80`;
- UD remains `UNCHANGED_PROTECTED`;
- UD-1, UD-2, and UD-3 remain unactivated research history;
- RTM remains `RTM_0_TRUTHFUL_NEUTRAL`, MRD remains
  `MRD-M0_DISPLAY_ONLY`, and all outputs remain `actionable=false`.

The durable Owner decision is under `acceptance/`. This revision changes no
production formula, source, bundle, runtime HTML, authority data, HOLD, RULE,
SQLite, or actionable state.
